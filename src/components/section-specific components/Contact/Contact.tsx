import { FC } from "react";
import { sectionIds } from "../../../constants/constants";

const Contact: FC = () => {
  return <section id={sectionIds.CONTACT}>Contact</section>;
};
export default Contact;
