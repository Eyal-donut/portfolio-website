// import { createSlice } from "@reduxjs/toolkit";
// import type { PayloadAction } from "@reduxjs/toolkit";

// interface targetHTML {
//   targetHTML: HTMLDivElement | null;
// }
// const initialState: targetHTML = {
//   targetHTML: null,
// };

// const targetHTMLSlice = createSlice({
//   name: "targetHTML",
//   initialState,
//   reducers: {
//     setTargetHTML: (state, action: PayloadAction<HTMLDivElement>) => {
//       return {
//         ...state,
//         targetRef: action.payload,
//       };
//     },
//   },
// });

// export const { setTargetHTML } = targetHTMLSlice.actions;

// export default targetHTMLSlice.reducer;
